####################
# Tutorial loading #
####################

# The tutorial file is not in one of the Info standard locations, its directory
# is appended to Info as a command-line argument.

Execute (Load up the tutorial):
  Info info.vim

Then (Check that the file matches the tutorial URI):
  AssertEqual 'info://info.vim.info/Top/', expand('%')
  
