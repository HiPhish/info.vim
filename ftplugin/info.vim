" Line numbers are undesired in documentation
" TODO: This should only be set if the file was opened by the 'Info' command
" setlocal nonumber

" This one serves to separate nodes of the file.
" TODO: Hide the <C-_> character on a single line, regex: /^$/

" I don't know yet what to do with the node header line
